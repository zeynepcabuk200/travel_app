import 'package:flutter/material.dart';

class BarItemView extends StatelessWidget {
  const BarItemView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(child: Text("bar item page"),),
    );
  }
}